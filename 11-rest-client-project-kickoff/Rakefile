require_relative './config/environment'

desc 'Run the CLI'
task :run do
    CLI.run
end

desc 'Other rake task'
task :other_rake_task do
    CLI.run
end

desc 'See Seattle subrreddit reddit posts'
task :reddit do
    RedditCLI.run
end

desc 'See a random cat fact'
task :cat_fact do
    CatFactsCLI.run
end